{
  "name": "puredown",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:server": "pnpm --filter @puredown/server dev",
    "dev:web": "pnpm --filter @puredown/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
